# Проверка 18.09.2026

- Два набора `tests/core.test.cjs` проходят: 20 тарифных сочетаний каждый и проверки новой/старой атрибуции, истечения срока, фильтра параметров, ограничения длины и обозначения варианта.
- `node --check app.js` пройден для обоих проектов.
- Статическая проверка: по 2 HTML-страницы, существующие локальные файлы и якоря, уникальные ID, один h1 на страницу, noindex, отключённый счётчик по умолчанию.
- В Chrome просмотрены первые экраны на широком экране и телефоне 390×844; проверена ширина 320 px. Найденное переполнение светлого заголовка исправлено.
- В браузере проверены изменение тарифа (Комфорт, выходной, 6 часов = 1 060 ₽), ночная акция 800 ₽ с условием 18+ после 22:00, раскрытие FAQ, галерея и закрытие, мобильное меню.
- Проверена новая ссылка с `utm_source=vk&rb_clickid=qa-new`: в ссылке LANGAME есть новый источник и вариант C; предыдущая `utm_campaign` удалена.
- Независимое чтение кода не выявило критических или важных дефектов для демопубликации. Два замечания исправлены: навигация без JS на телефоне и отправка variant_view со страницы политики.

Ограничения: оплата/бронь в LANGAME не выполнялась; фактические Core Web Vitals, Safari/Firefox, вспомогательные технологии и сквозная аналитика оплаты не измерялись. Полноценный без-JS прогон не выполнялся; запасной путь проверен по HTML/CSS.

Проверена внешняя страница LANGAME: открывается форма входа/регистрации. Авторизация и создание заказа не выполнялись.
